import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['tong.visitkorea.or.kr', 'lh3.googleusercontent.com', 'images.unsplash.com'],
  },
  /* config options here */
};

export default nextConfig;
